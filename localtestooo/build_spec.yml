version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "testValue1"
  exportedVariables:
    - testEnv
    - patch_number
    - build_Result
    - test_env
    - test_env1

inputArtifacts:
  - name: hello-dev-jar
    location: /workspace/Source/hello123.class

steps:
